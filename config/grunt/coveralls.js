module.exports = function () {
  'use strict';

  return {
    options: {
      debug: false,
      dryRun: false,
      coverageDir: 'coverage',
      force: true,
      recursive: true
    }
  };
};